import { createActions } from 'redux-actions';

const actionCreators = createActions({
    VIOLATION: {
        ORDINARY_INFO_ADD: {
            SAVE_MESSAGE: void 0,
            CHANGE_VALUE: void 0
        }
    }
});
export default actionCreators.violation.ordinaryInfoAdd;
